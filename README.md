A project created with Claude Code and testing its capabilities. A catalog of Hot Wheels car parameters.

## Getting Started

First, setup project:

```bash
npm run setup
```

Then, run the development server:

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.